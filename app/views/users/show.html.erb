<h3>Welcome, <%= @user.email %></h3>

<% if @user.messages.nil? %>
  <p> No sent messages </p>
<% else %>
  <ul>
    <% @user.messages.each do |msg| %>
      <li><%= link_to "Message sent to #{msg.to}", user_message_path(@user, msg) %></li>
    <% end %>
  </ul>
<% end %>
